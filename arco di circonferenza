var R = 3;

var sin = function(v){
	return [v[1]*SIN(v[0])/2];
};

var cos = function(v){
	return [v[1]*COS(v[0])/2];
};


var mappings = [cos,sin];

var domain = DOMAIN([[0,PI/2],[2,R]])([36,50]);

var model = MAP(mappings)(domain);


DRAW(model);
